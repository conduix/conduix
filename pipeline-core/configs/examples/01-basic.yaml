# 기본 예제: Kafka → 콘솔 출력

name: "basic-pipeline"

input:
  type: kafka
  brokers: ["localhost:9092"]
  topics: ["events"]

steps:
  - name: parse
    transform: |
      root = this.parse_json()

  - name: print
    save:
      type: stdout
