import { Typography } from 'antd'

const { Title } = Typography

export default function HistoryPage() {
  return (
    <div>
      <Title level={4}>실행 히스토리</Title>
      <p>전체 파이프라인 실행 히스토리가 표시될 예정입니다.</p>
    </div>
  )
}
